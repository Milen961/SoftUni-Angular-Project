import { EmailDirective  } from './email-check-directive';

describe('EmailDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailDirective();
    expect(directive).toBeTruthy();
  });
});
